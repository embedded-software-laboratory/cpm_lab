#pragma once

#include <string>
#include <vector>
#include <chrono>
#include <tuple>

using std::vector;
using std::string;
using std::tuple;
using time_point = std::chrono::high_resolution_clock::time_point;
using std::chrono::high_resolution_clock;
